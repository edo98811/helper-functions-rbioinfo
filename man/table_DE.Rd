% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_DE.R
\name{table_DE}
\alias{table_DE}
\title{Generate a DataTable for Differentially Expressed Genes}
\usage{
table_DE(contrast, myresuSet, report = FALSE)
}
\arguments{
\item{contrast}{A character string specifying the contrast name to retrieve results for.}

\item{myresuSet}{A list containing results sets, where each element corresponds to a contrast.
Each contrast should contain a data frame named \code{etbl_res_DE} with the results.}

\item{report}{Logical. If \code{TRUE}, the function outputs the DataTable in a format suitable
for inclusion in reports (e.g., using \code{knitr::knit_print}). If \code{FALSE}, the DataTable
is printed directly to the console.}
}
\value{
The input \code{myresuSet} list is returned unchanged.
}
\description{
This function creates a DataTable for visualizing differentially expressed genes
from a given contrast within a results set. If no differentially expressed genes
are found, a message is displayed suggesting to increase the FDR threshold.
}
\examples{
# Example usage:
# Assuming `myresuSet` is a list containing results for contrasts:
# myresuSet <- list(
#   contrast1 = list(etbl_res_DE = data.frame(gene = c("gene1", "gene2"), logFC = c(1.2, -0.8))),
#   contrast2 = list(etbl_res_DE = NULL)
# )
table_DE("contrast1", myresuSet, report = FALSE)

}
