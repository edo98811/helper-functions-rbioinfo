% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_missing_values.R
\name{plot_missing_values}
\alias{plot_missing_values}
\title{Plot Missing Values in a Dataset}
\usage{
plot_missing_values(se)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} object or a matrix/data frame containing the dataset.}
}
\value{
A ggplot object visualizing the missing values in the dataset.
}
\description{
This function visualizes the missing values in a dataset using the \code{visdat} package.
}
\examples{
# Example with a SummarizedExperiment object
library(SummarizedExperiment)
se <- SummarizedExperiment(assays = list(counts = matrix(rnorm(100), nrow = 10)))
plot_missing_values(se)

# Example with a matrix
mat <- matrix(rnorm(100), nrow = 10)
plot_missing_values(mat)
}
